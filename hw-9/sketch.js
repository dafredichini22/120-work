//Frederick Cantarine, 50

function setup() {
    createCanvas(windowWidth, 700);
    background(255);

  //  frameRate(200);
  }

function draw() {
  var col1 = 255;
  var col2 = 255;
  var col3 = 0;
  var col4 = 50;

  var x = 0;
  var y = 20;



  //  fill(col1 + x, col1 + x, 0 );
    ellipse (200, y, 50);

    y = y - 50;
/*
    if (col1 == 255){
      x =  x - 50;
    }
    else {
    x = x + 50;

    }
    */

/*
    if (col1 == col2) {
      col1 -= 205;
    }
    else {
      col1 +=205;
    }

*/

}
